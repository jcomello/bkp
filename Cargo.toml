[package]
name = "bkp"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0.104"
chrono = { version = "0.4.45", default-features = false, features = ["std", "now"] }
clap = { version = "4.6.6", features = ["derive"] }
